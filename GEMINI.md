# Gemini Workspace Rules & Guidelines

## 📁 프로젝트 폴더 구조 지침
1. **루트 디렉터리 역할**:
   * 이 폴더(`03_Gemini`)는 모든 Gemini 코딩 프로젝트를 총괄하는 **최상위 루트 디렉터리**입니다.
   * `03_Gemini` 루트에 개별 프로젝트 소스 코드 파일(예: `.py`, `.js`, `.html` 등)을 직접 생성하지 않습니다.

2. **개별 프로젝트 하위 폴더 격리**:
   * 새로운 프로젝트나 작업을 진행할 때는 반드시 `03_Gemini` 내에 번호가 매겨진 하위 폴더를 생성하여 관리합니다.
   * 폴더 명명 규칙: `01. FINANCE TEST`, `02. <프로젝트명>`, `03. <프로젝트명>` 등
   * 프로젝트에 필요한 모든 소스 코드, 설정 파일(`requirements.txt`, `.gitignore` 등), 문서는 해당 하위 폴더 내에만 저장합니다.

3. **명령어 및 서버 실행 기준**:
   * 터미널 명령어 실행, 패키지 설치, 로컬 서버 구동 시 작업 디렉터리(Working Directory)를 반드시 해당 하위 프로젝트 폴더로 지정하여 실행합니다.

---

## 💬 대화 및 작업 스타일
- 사용자와의 모든 소통은 **한국어**로 명확하고 친절하게 진행합니다.
- 변경 작업 시 기존 프로젝트들의 파일이 섞이지 않도록 경로를 철저히 검증합니다.
